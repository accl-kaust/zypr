All IPCORES to be blackboxed must be wrapped in an interface module to expose the BB.

PR modules should exist as standalone files, i.e. define module and it's underlying hierarchy in a seperate file.